const personas=['willy', 'pepe', 'adri'];
personas.forEach(function(personas, index){
    // console.log(index);
    console.log(personas);
}
    
);
personas.forEach((personas, index)=>{
    console.log(personas);
});